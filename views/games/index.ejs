<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Gaming Vault</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Welcome to <%= user.username %>'s Game Vault!</h1>
    <ul>
        <% games.forEach(game => {  %>
            <li>
                <a href="/users/<%= user._id %>/videoGames/<%= game._id %>">
                    <%= game.title %>
                </a>
            </li>
        <% }) %>
    </ul>
</body>
</html>